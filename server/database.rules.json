{
  "rules": {
    "games": {
      "$gameId": {
        ".read": "data.child('players').child(auth.uid).val() === true"
      }
    },
    "hands": {
      "$userId": {
        ".read": "$userId === auth.uid"
      }
    }
  }
}
